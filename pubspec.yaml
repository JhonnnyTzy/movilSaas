name: paragit_movil
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  # Iconos estilo iOS
  cupertino_icons: ^1.0.8

  # Para conectar con tu Backend (Node.js)
  http: ^1.6.0

  # Para guardar el Token de sesión en el celular
  shared_preferences: ^2.5.4

  # Para usar fuentes bonitas de Google
  google_fonts: ^8.0.0
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Reglas de estilo de código
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Si en el futuro agregas imágenes, descomenta y usa estas líneas:
  # assets:
  #   - assets/images/logo.png